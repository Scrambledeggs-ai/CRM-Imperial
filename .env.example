SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Opcional: si la seteás, cada vez que se crea/edita/borra un contacto o post
# se manda un POST con un JSON a esta URL (útil para conectar a n8n, Airtable, etc.)
WEBHOOK_URL=
